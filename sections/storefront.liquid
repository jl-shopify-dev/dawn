<div class="storefront">
  <img class="storefront-image" src="{{ section.settings.storefront_image | img_url: 'master' }}">
  <div class="storefront-info ">
    <h2>{{ shop.name }}</h2>
    <p>{{ shop.description }}</p>
    <a href="{{ shop.secure_url }}">{{ shop.secure_url }}</a>
    <a href="mailto: {{ shop.email }}">{{ shop.email }}</a>
    <p>{{ shop.address.summary }}</p>
    <p>{{ shop.phone }}</p>
  </div>
</div>

{% schema %}
{
  "name": "Storefront",
  "class": "storefront-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "storefront_image",
      "label": "Storefront Image"
    }
  ],
  "presets": [
    {
      "name": "Storefront"
    }
  ]
}
{% endschema %}

{% stylesheet %}

.storefront {
  --storefront-height: 500px;
  position: relative;
  height: var(--storefront-height);
  
  <!-- border: solid 1px rgba(200, 200, 200, 0.1); -->
  /* box-shadow: 0 0 10px rgb(0 0 0 / 50%); */
}

.storefront-info {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-items: center;
  
  position: absolute;
  bottom: 50%;
  left: 50%;
  transform: translate(-50%, 50%);
  background-color: rgb(var(--color-base-accent-1));
  opacity: 1;
  
  width: 90vw;
  max-width: 800px;
  padding: 24px 10vw;
  border-radius: 2px;
  box-shadow: 0 0 4px 4px rgb(0 0 0 / 10%);
}
.storefront-info > * {
  color: white;
  text-align: center;
}

.storefront-image {
  width: 100%;
  height: 100%;
  margin: auto;
  object-fit: cover;
  object-position: center;
}

.menu-page-width {
  --max-page-width: 1140px;
  width: 100%;
  max-width: var(--max-page-width);
  padding-left: 1.5rem;
  padding-right: 1.5rem;
  margin-left: auto;
  margin-right: auto;
}

{% endstylesheet %}

{% javascript %}
{% endjavascript %}